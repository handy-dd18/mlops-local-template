# Pinned deps for the dbt runner container.
# This image also runs the S3->RDS loader and the training pipeline
# (per Makefile contract), so it carries boto3/pandas/sqlalchemy/sklearn
# in addition to dbt itself.
#
# Version note: dbt-postgres 1.10.0 (the latest published as of 2026-05)
# transitively pulls dbt-core 1.11.x. Older dbt-core 1.10 line is
# incompatible with the modern dbt-adapters (protobuf 6.x) that
# dbt-postgres 1.10.0 requires, so we let pip resolve dbt-core itself.
dbt-postgres==1.10.0
boto3==1.43.6
pandas==2.3.3
psycopg2-binary==2.9.12
SQLAlchemy==2.0.44
scikit-learn==1.7.2
mlflow==3.12.0
